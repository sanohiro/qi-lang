;; utils/slug.qi - スラッグ生成ユーティリティ

(module "slug")

;; スラッグ生成（タイトルからURL安全な文字列を作成）
(defn make-slug [title]
  (let [lower (str/lower title)
        dashed (str/replace lower " " "-")
        cleaned (str/replace dashed "[^a-z0-9-]" "")]
    cleaned))

(export make-slug)
